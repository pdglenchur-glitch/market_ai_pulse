# Placeholder — built out in Phase 2 (Section 9 of PROJECT_PLAN.md)
name: pipeline
on:
  workflow_dispatch: {}

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "pipeline.yml not yet implemented — see PROJECT_PLAN.md Phase 2"
